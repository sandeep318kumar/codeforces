#include<bits/stdc++.h>
using namespace std;

void solve()
{
    int n, m;
    cin>>n>>m;
    vector<int>A(n), B(n), C(m);
    // plan colors
    for(int i=0;i<n;i++)
        cin>>A[i];
    // color you want
    for(int i=0;i<n;i++)
        cin>>B[i];
    // color painter have
    for(int i=0;i<m;i++)
        cin>>C[i];

     

}
int main()
{
    int t;
    cin>>t;
    while(t--){ 
        solve();
    }
    return 0;
}
